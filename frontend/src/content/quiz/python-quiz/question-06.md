---
quizName: "Python Quiz"
type: "multiple-choice"
timeLimit: 15
points: 1000
correctAnswer: ["Automatically closes the file even if an exception occurs"]
options: [
  "It's faster to read files",
  "Automatically closes the file even if an exception occurs",
  "Allows reading and writing simultaneously",
  "Prevents other programs from accessing the file"
]
---

What is the main advantage of using

```python
with open(filename) as f:
    # Do something with the file
```

instead of just `f = open(filename)`?
