---
quizName: "Python Quiz"
type: "multiple-choice"
timeLimit: 10
points: 500
correctAnswer: ["def add(a: int, b: int) -> int:"]
options: [
  "def add(a: int, b: int) -> int:",
  "def add(int a, int b) -> int:",
  "def add(a, b) -> int: int:",
  "def add<int>(a, b) -> int:"
]
---

Which of the following is the **correct** syntax for type hints in a function that takes two integers and returns an integer?
