---
quizName: "Python Quiz"
type: "multiple-choice"
timeLimit: 15
points: 750
correctAnswer: ["Triple-quoted string right after the function definition"]
options: [
  "Triple-quoted string right after the function definition",
  "Comment with # after the function name",
  "String in the return statement",
  "Comment with // before the function"
]
---

What is the **correct** way to write a docstring for a function in Python?

```python
def calculate_area(radius):
    # Where should the docstring go?
    return 3.14 * radius ** 2
```
