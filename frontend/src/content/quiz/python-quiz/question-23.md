---
quizName: "Python Quiz"
type: "multiple-choice"
timeLimit: 10
points: 250
correctAnswer: ["str"]
options: ["str", "int", "float", "any"]
---

What data type does the `input()` function always return?

```python
age = input("Enter your age: ")
print(type(age).__name__)
```
