---
quizName: "Python Quiz"
type: "multiple-choice"
timeLimit: 10
points: 250
correctAnswer: ["math.sqrt(16)"]
options: ["math.sqrt(16)", "sqrt(16)", "Both work", "Neither works"]
---

Given the import statement:

```python
import math
```

Which of the following is the **correct** way to use the `sqrt` function?
