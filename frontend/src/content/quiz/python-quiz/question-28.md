---
quizName: "Python Quiz"
type: "multiple-choice"
timeLimit: 15
points: 500
correctAnswer: ["Initialize instance attributes when an object is created"]
options: [
  "Initialize instance attributes when an object is created",
  "Define the class itself",
  "Delete an object when it's no longer needed",
  "Import modules needed by the class"
]
---

What is the primary purpose of the `__init__` method in a Python class?

```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
```
